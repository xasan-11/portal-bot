# Base URL of the backend API/bot service, e.g. https://your-backend.up.railway.app
# Leave unset for local dev (Vite's dev-server proxy handles it) or if this
# dashboard is served by the same process as the backend.
# Vite inlines this at BUILD time — changing it requires a rebuild, not just a redeploy restart.
VITE_API_URL=
